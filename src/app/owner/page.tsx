/**
 * The dashboard tab.
 *
 * Renders nothing itself: the console around it is mounted by the layout, so
 * that it survives navigation between tabs instead of being rebuilt on each
 * one. This page exists to give /owner a route; which tab shows is read from
 * the URL. See OwnerConsoleFrame.
 */
export default function OwnerDashboardPage() {
  return null;
}
